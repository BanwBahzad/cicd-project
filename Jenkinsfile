pipeline {
agent any

stages {
    stage('Build') {
        steps {
            echo 'Build started'
            echo 'Jenkins is running successfully'
        }
    }
}

post {
    always {
        echo 'Pipeline finished'
    }
}


}
